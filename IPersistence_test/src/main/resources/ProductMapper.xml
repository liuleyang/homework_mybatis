<mapper namespace="product">
    <select id="selectList" resultType="">
        select * from product
    </select>

    <select id="selectOne">
        select * from product where id =     and  username =
    </select>
</mapper>